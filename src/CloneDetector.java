import java.util.Map;
import java.util.Set;

/**
 * 
 */

/**
 * @author vaibhavsaini
 *
 */
public class CloneDetector {
    /**
     * main method
     * @param args
     */
    public static void main(String args[]){
        
    }
    
    /**
     * setA and setB represents two projects. Each element of the set is a map of
     * Token as a key and it's frequency as value
     * the method compares the 
     * @param setA set of hashmaps of tokens and their method_frequency
     * @param setB set of hashmaps of tokens and their method_frequency
     */
    private void detectClones(Set<Map<Token,Integer>> setA, Set<Map<Token,Integer>> setB){
        
    }

}
